[project]
name = "autopep2"
version = "0.1.0"
description = "Terminal OpenAI Agents SDK chatbot for Autopep protein workflows"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "openai-agents>=0.7.0",
    "python-dotenv>=1.0.1",
]
